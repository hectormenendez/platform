module.exports = {
    extends: ["./config-react.js"],

    plugins: ["react-native"],

    rules: {
        // ---------------------------------------------------------------------- React-Native Plugin

        "react-native/no-inline-styles": "error",

    }
}
