{
    "private": true,
    "name": "root",
    "engines": {
        "node": ">=12.16.0"
    },
    "os": [
        "darwin",
        "linux"
    ],
    "husky": {
        "hooks": {
            "pre-commit": "NODE_ENV=production lint-staged"
        }
    },
    "lint-staged": {
        "*.{js,jsx}": [
            "eslint --fix"
        ],
        "*.svelte": [
            "eslint --fix",
            "prettier --write --plugin-search-dir=."
        ],
        "./data/schema.mmd": [
            "lerna run build:schema --scope=data --no-prefix --loglevel=error --",
            "git add ./data/.files/schema.svg"
        ]
    },
    "scripts": {
        "postinstall": "lerna bootstrap",
        "start": "lerna run parent:start --stream --parallel"
    },
    "devDependencies": {
        "eslint": "^7.3.1",
        "husky": "^4.2.5",
        "lerna": "^3.22.1",
        "lint-staged": "^10.2.11",
        "prettier": "2.0.5"
    }
}
